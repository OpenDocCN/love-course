# 🌟 Python基础教程 🌟

## 概述

在本节课中，我们将学习Python编程语言的基础知识，包括变量、数据类型、运算符和基本控制结构。

## 变量与数据类型

### 变量

变量是存储数据的容器。在Python中，变量不需要声明类型，直接赋值即可。

```python
x = 10
y = "Hello, World!"
```

### 数据类型

Python中有多种数据类型，包括数字、字符串和布尔值。

- **数字**：用于表示数值，例如整数（int）和浮点数（float）。

```python
a = 5
b = 3.14
```

- **字符串**：用于表示文本，由引号包围。

```python
name = "Alice"
```

- **布尔值**：用于表示真（True）或假（False）。

```python
is_valid = True
```

## 运算符

运算符用于对变量进行操作。以下是一些常见的运算符：

- **算术运算符**：用于执行算术运算，例如加法（+）、减法（-）、乘法（*）和除法（/）。

```python
result = a + b
```

- **比较运算符**：用于比较两个值，例如等于（==）、不等于（!=）、大于（>）和小于（<）。

```python
is_equal = a == b
```

- **逻辑运算符**：用于执行逻辑运算，例如与（and）、或（or）和非（not）。

```python
is_valid_and_not_empty = is_valid and name != ""
```

## 控制结构

控制结构用于控制程序的执行流程。

### 条件语句

条件语句用于根据条件执行不同的代码块。

```python
if a > b:
    print("a is greater than b")
elif a < b:
    print("a is less than b")
else:
    print("a is equal to b")
```

### 循环语句

循环语句用于重复执行代码块。

- **for循环**：用于遍历序列（例如列表、元组、字符串）。

```python
for i in range(5):
    print(i)
```

- **while循环**：用于在满足条件时重复执行代码块。

```python
i = 0
while i < 5:
    print(i)
    i += 1
```

## 总结

本节课中我们一起学习了Python编程语言的基础知识，包括变量、数据类型、运算符和基本控制结构。希望这些知识能帮助你更好地理解Python编程。