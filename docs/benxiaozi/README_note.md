# 📚 数据结构与算法基础教程 1.1

## 概述
在本节课中我们将要学习数据结构的基本概念和算法的基本思想。

## 数据结构简介
数据结构是计算机存储、组织数据的方式。它决定了数据的存储方式、访问方式和修改方式。

### 常见的数据结构
以下是常见的数据结构：

- **数组（Array）**：一种线性数据结构，用于存储一系列元素。
  - **公式**：`A[i]` 表示数组 A 中第 i 个元素。
- **链表（Linked List）**：一种线性数据结构，由一系列节点组成，每个节点包含数据和指向下一个节点的指针。
  - **代码**：```python
    class Node:
        def __init__(self, data):
            self.data = data
            self.next = None

    class LinkedList:
        def __init__(self):
            self.head = None

        def append(self, data):
            new_node = Node(data)
            if not self.head:
                self.head = new_node
                return
            last_node = self.head
            while last_node.next:
                last_node = last_node.next
            last_node.next = new_node
  ```
- **栈（Stack）**：一种后进先出（LIFO）的数据结构。
  - **公式**：`push(data)` 添加元素，`pop()` 移除元素。
- **队列（Queue）**：一种先进先出（FIFO）的数据结构。
  - **公式**：`enqueue(data)` 添加元素，`dequeue()` 移除元素。

## 算法简介
算法是一系列解决问题的步骤。它可以是简单的，也可以是复杂的。

### 常见的算法
以下是常见的算法：

- **排序算法**：用于对数据进行排序。
  - **代码**：```python
    def bubble_sort(arr):
        n = len(arr)
        for i in range(n):
            for j in range(0, n-i-1):
                if arr[j] > arr[j+1]:
                    arr[j], arr[j+1] = arr[j+1], arr[j]
  ```
- **搜索算法**：用于在数据结构中查找特定元素。
  - **代码**：```python
    def linear_search(arr, x):
        for i in range(len(arr)):
            if arr[i] == x:
                return i
        return -1
  ```
- **递归算法**：一种解决问题的方法，通过将问题分解为更小的子问题来解决。
  - **代码**：```python
    def factorial(n):
        if n == 0:
            return 1
        return n * factorial(n-1)
  ```

## 总结
本节课中我们一起学习了数据结构和算法的基本概念。希望这些知识能帮助你更好地理解计算机科学。