# 🚦 男哥内核3.0初级班：内部课 22.飞龙在天系统延伸拓展（七）红绿灯系统（下）

## 概述
在本节课中，我们将继续探讨飞龙在天系统的红绿灯系统，深入了解其工作原理和实现方法。

## 红绿灯系统工作原理
红绿灯系统是飞龙在天系统中用于控制流程的关键组件。它通过定义不同的状态和转换规则来管理程序的执行流程。

### 状态定义
红绿灯系统中的状态通常包括：
- **红灯**：表示停止或等待状态。
- **绿灯**：表示运行或执行状态。
- **黄灯**：表示准备或过渡状态。

### 转换规则
状态之间的转换规则如下：
- **红灯到绿灯**：当满足特定条件时，系统从红灯状态转换为绿灯状态。
- **绿灯到黄灯**：当满足特定条件时，系统从绿灯状态转换为黄灯状态。
- **黄灯到红灯**：当满足特定条件时，系统从黄灯状态转换为红灯状态。

## 红绿灯系统实现方法
以下是一个简单的红绿灯系统实现示例：

```python
class TrafficLight:
    def __init__(self):
        self.state = "RED"

    def change_state(self):
        if self.state == "RED":
            self.state = "GREEN"
        elif self.state == "GREEN":
            self.state = "YELLOW"
        elif self.state == "YELLOW":
            self.state = "RED"

    def get_state(self):
        return self.state
```

### 使用示例
```python
light = TrafficLight()
print(light.get_state())  # 输出：RED
light.change_state()
print(light.get_state())  # 输出：GREEN
light.change_state()
print(light.get_state())  # 输出：YELLOW
light.change_state()
print(light.get_state())  # 输出：RED
```

## 总结
本节课中，我们学习了飞龙在天系统的红绿灯系统，了解了其工作原理和实现方法。通过简单的示例，我们掌握了如何使用红绿灯系统来控制程序的执行流程。