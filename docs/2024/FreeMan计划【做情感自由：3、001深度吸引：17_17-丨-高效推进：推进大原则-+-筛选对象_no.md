# 课程01：Python基础入门 🐍

在本节课中，我们将要学习Python编程语言的基础知识。课程将从最核心的概念“变量”开始，逐步介绍如何存储和操作数据，最后学习如何通过“输入/输出”与程序进行交互。这些是编写任何Python程序的基石。

## 变量：数据的容器 📦

上一节我们介绍了课程目标，本节中我们来看看什么是变量。变量是编程中用于存储数据的容器。你可以把它想象成一个贴有标签的盒子，标签是变量名，盒子里装的是数据。

在Python中，创建一个变量并为其赋值非常简单，只需要使用等号 `=`。

以下是创建变量的基本语法：
```python
变量名 = 值
```

例如：
```python
name = "小明"  # 存储一个字符串
age = 25      # 存储一个整数
height = 1.75 # 存储一个小数（浮点数）
```

**核心概念**：赋值操作 `变量名 = 值` 的作用是将等号右边的“值”存入左边的“变量”中。

## 数据类型：数据的种类 🔢

我们已经知道如何用变量存储数据，但数据本身有不同的类型。Python中常见的基本数据类型有以下几种。

以下是主要的数据类型列表：
*   **整数 (int)**： 表示没有小数部分的数字，如 `10`, `-5`, `0`。
*   **浮点数 (float)**： 表示带有小数点的数字，如 `3.14`, `2.0`, `-0.5`。
*   **字符串 (str)**： 表示文本信息，需要用单引号 `‘ ’` 或双引号 `“ ”` 括起来，如 `‘你好’`, `“Python”`。
*   **布尔值 (bool)**： 表示逻辑真或假，只有两个值：`True` 和 `False`。

了解数据类型很重要，因为它决定了你能对数据做什么操作。例如，数字可以进行加减乘除，而字符串可以进行拼接。

## 基本运算：操作数据 ➕➖✖️➗

掌握了数据和它们的类型后，我们就可以对它们进行操作了。Python支持多种基本运算。

以下是常见的运算符：
*   **算术运算符**：用于数学计算。
    *   `+` 加法
    *   `-` 减法
    *   `*` 乘法
    *   `/` 除法（结果是浮点数）
    *   `//` 整除（只取整数部分）
    *   `%` 取模（求余数）
    *   `**` 幂运算
*   **比较运算符**：用于比较两个值，结果是布尔值 (`True` 或 `False`)。
    *   `==` 等于
    *   `!=` 不等于
    *   `>` 大于
    *   `<` 小于
    *   `>=` 大于等于
    *   `<=` 小于等于

**核心概念**：运算表达式 `a + b` 会根据 `a` 和 `b` 的类型产生结果。数字相加是数学和，字符串相加是文本拼接。

## 输入与输出：与程序对话 💬

最后，我们来学习如何让程序与用户交流。这通过输入和输出功能实现。

**输出**使用 `print()` 函数，它可以将括号内的内容显示在屏幕上。
```python
print(“Hello, World!”) # 输出文本
print(age)             # 输出变量的值
```

**输入**使用 `input()` 函数，它会暂停程序，等待用户在键盘上输入一些内容，然后以字符串的形式返回。
```python
user_name = input(“请输入你的名字： “)
```
当用户输入并按下回车后，输入的内容就会被存储在 `user_name` 这个变量中。

**核心概念**：`input()` 函数总是返回字符串。如果你需要数字，需要使用 `int()` 或 `float()` 函数进行转换。
```python
age_str = input(“请输入你的年龄： “) # age_str 是字符串，如 “25”
age = int(age_str)                  # 将字符串转换为整数 25
```

---

本节课中我们一起学习了Python编程的基础核心概念。我们认识了**变量**作为数据的容器，了解了不同的**数据类型**，学会了使用运算符进行**基本运算**，并掌握了通过 `print` 和 `input` 实现**输入与输出**。理解这些内容是编写更复杂程序的坚实基础。