# leon《全套课程合集》：Leon2.0：2.什么是搭讪_note

在本节课中，我们将要学习Python编程语言的基础知识，包括变量、数据类型和基本操作。这些是编写任何Python程序的基石。

## 什么是Python？ 🤔

Python是一种高级、解释型的编程语言。它以简洁易读的语法而闻名。Python的设计哲学强调代码的可读性。

## 变量与赋值 📦

上一节我们介绍了Python语言的基本概念，本节中我们来看看如何在Python中存储数据。

变量是存储数据的容器。在Python中，使用等号 `=` 为变量赋值。变量名可以是字母、数字和下划线的组合，但不能以数字开头。

以下是变量赋值的基本语法：
```python
variable_name = value
```

例如：
```python
name = "Alice"
age = 25
height = 1.75
```

## 基本数据类型 🔢

Python有几种内置的核心数据类型，用于表示不同种类的信息。

以下是主要的数据类型列表：
*   **整数 (int)**： 表示没有小数部分的数字，例如 `10`, `-5`, `0`。
*   **浮点数 (float)**： 表示带有小数点的数字，例如 `3.14`, `2.0`, `-0.5`。
*   **字符串 (str)**： 表示文本信息，用单引号 `' '` 或双引号 `" "` 包围，例如 `"Hello"`, `'Python'`。
*   **布尔值 (bool)**： 表示逻辑值，只有 `True` 和 `False` 两个值。

## 基本操作符 ➕➖✖️➗

了解了如何存储数据后，我们来看看如何操作这些数据。

Python支持各种操作符来执行算术、比较和逻辑运算。

以下是常用的操作符分类：
*   **算术操作符**： 用于数学计算。
    *   `+` 加法
    *   `-` 减法
    *   `*` 乘法
    *   `/` 除法（结果是浮点数）
    *   `//` 整除（结果是整数）
    *   `%` 取模（求余数）
    *   `**` 幂运算
*   **比较操作符**： 用于比较两个值，返回布尔值 (`True` 或 `False`)。
    *   `==` 等于
    *   `!=` 不等于
    *   `>` 大于
    *   `<` 小于
    *   `>=` 大于等于
    *   `<=` 小于等于
*   **逻辑操作符**： 用于组合布尔条件。
    *   `and` 逻辑与（两者都为真则结果为真）
    *   `or` 逻辑或（至少一个为真则结果为真）
    *   `not` 逻辑非（取反）

## 总结 📝

本节课中我们一起学习了Python编程的基础。我们了解了Python语言的特点，学会了如何使用变量来存储数据，认识了整数、浮点数、字符串和布尔值这几种基本数据类型，并掌握了算术、比较和逻辑操作符的使用方法。这些概念是后续学习更复杂Python编程的坚实基础。